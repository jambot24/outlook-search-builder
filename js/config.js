// Public site configuration.
// msalClientId is the Application (client) ID of the Entra app registration used for the
// optional "Sign in with Microsoft" folder picker. It is a public identifier, not a secret:
// every single-page app ships it to the browser. Leave it empty to hide sign-in entirely.
window.OSB_CONFIG = {
  msalClientId: '',
};
